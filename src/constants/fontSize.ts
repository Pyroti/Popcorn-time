import { moderateScale } from 'react-native-size-matters';

export const fontSize = {
  small: `${moderateScale(12)}px`,
  regular: `${moderateScale(14)}px`,
  large: `${moderateScale(20)}px`,
  extralarge: `${moderateScale(29)}px`,
};
