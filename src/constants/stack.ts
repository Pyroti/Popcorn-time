export enum AuthStack {
  SignIn = '[Auth] Sign In',
  SignUp = '[Auth] Sign Up',
}

export enum TabStack {
  Home = '[Tab] Home',
  Profile = '[Tab] Profile',
}

export enum MainStack {
  Tab = '[Main] Tab',
}
